'use client';

import s from './Slider.module.scss';
import { useEffect } from 'react';

const Slider = ({ current }: { current: number }) => {

	useEffect(() => {

	}, [current]);

	return (
		<div>

		</div>
	);
};

export default Slider;
